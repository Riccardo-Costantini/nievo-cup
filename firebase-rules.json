{
  "rules": {
    ".read": false,
    ".write": false,
    "matches": {
      ".read": true,
      "$matchId": {
        ".write": "auth != null && auth.token.admin === true",
        ".validate": "newData.hasChildren(['id','date','time','home','away']) && newData.child('id').isString() && newData.child('id').val() === $matchId && newData.child('date').isString() && newData.child('time').isString() && newData.child('home/name').isString() && newData.child('away/name').isString() && (!newData.child('round').exists() || newData.child('round').isString()) && (!newData.child('group').exists() || newData.child('group').isString())"
      }
    },
    "liveResults": {
      ".read": true,
      "$matchId": {
        ".write": "auth != null && auth.token.admin === true",
        ".validate": "newData.hasChildren(['scorersHome','scorersAway','status','updatedAt']) && newData.child('status').isString() && (newData.child('status').val() === 'In programma' || newData.child('status').val() === 'Live' || newData.child('status').val() === 'Finita') && newData.child('updatedAt').isNumber()"
      }
    },
    "admins": {
      ".read": "auth != null && auth.token.admin === true",
      ".write": false
    }
  }
}
